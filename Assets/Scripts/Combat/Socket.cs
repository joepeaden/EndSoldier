using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Socket : MonoBehaviour
{
	// bool isOccupied = false;	
	// GameObject encumbent;

	// public bool IsOccupied()
	// {
	// 	return isOccupied;
	// }

	// public void EmptySocket()
	// {
	// 	encumbent = null;
	// 	isOccupied = false;
	// }

	// public GameObject OccupiedBy(GameObject g)
	// {
	// 	// I don't think I've ever used this word!
	// 	encumbent = g;
	// 	isOccupied = true;

	// 	return gameObject;
	// }

	// void OnTriggerEnter2D(Collider2D other)
	// {
	// 	Hostile h = other.gameObject.GetComponent<Hostile>();
	// 	if(h != null)
	// 	{
	// 		if(h.targetSocket.Equals(gameObject))
	// 			h.ReachedTargetZone(true);
	// 	}
	// }

	// void OnTriggerExit2D(Collider2D other)
	// {
	// 	Hostile h = other.gameObject.GetComponent<Hostile>();
	// 	if(h != null)
	// 	{
	// 		if(h.targetSocket.Equals(gameObject))
	// 			h.ReachedTargetZone(false);
	// 	}
	// }
}
